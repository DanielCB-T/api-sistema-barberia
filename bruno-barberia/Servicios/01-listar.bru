meta {
  name: Listar servicios (público)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/services
  body: none
  auth: none
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
