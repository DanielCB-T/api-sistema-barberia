meta {
  name: Ver servicio
  type: http
  seq: 2
}

get {
  url: {{base_url}}/services/{{service_id}}
  body: none
  auth: none
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
