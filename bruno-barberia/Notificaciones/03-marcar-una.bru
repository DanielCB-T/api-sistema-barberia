meta {
  name: Marcar una como leída
  type: http
  seq: 3
}

patch {
  url: http://177.7.32.156/api-sistema-barberia/api/notifications/{{notification_id}}/read
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("200, o 403/404 segun pertenencia/existencia", function() {
    expect([200,403,404]).to.include(res.getStatus());
  });
}
