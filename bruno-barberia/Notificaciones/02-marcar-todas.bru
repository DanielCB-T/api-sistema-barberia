meta {
  name: Marcar todas como leídas
  type: http
  seq: 2
}

post {
  url: http://177.7.32.156/api-sistema-barberia/api/notifications/read-all
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
