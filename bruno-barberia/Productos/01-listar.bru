meta {
  name: Listar productos (público)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/products?per_page=12
  body: none
  auth: none
}

params:query {
  per_page: 12
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
