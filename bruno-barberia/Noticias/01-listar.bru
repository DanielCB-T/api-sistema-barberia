meta {
  name: Listar noticias (público)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/news
  body: none
  auth: none
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
