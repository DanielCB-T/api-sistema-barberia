meta {
  name: Ver noticia
  type: http
  seq: 2
}

get {
  url: {{base_url}}/news/{{news_id}}
  body: none
  auth: none
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
