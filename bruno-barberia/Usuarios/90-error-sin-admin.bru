meta {
  name: ERROR listar sin rol admin (403)
  type: http
  seq: 90
}

get {
  url: http://177.7.32.156/api-sistema-barberia/api/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{client_token}}
}

tests {
  test("status 403 (prohibido para cliente)", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
