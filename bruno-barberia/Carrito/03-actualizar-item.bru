meta {
  name: Actualizar cantidad del item
  type: http
  seq: 3
}

put {
  url: http://177.7.32.156/api-sistema-barberia/api/cart/items/{{cart_item_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{client_token}}
}

body:json {
  {
    "quantity": 2
  }
}

tests {
  test("200, o 422/404 segun stock/existencia", function() {
    expect([200,422,404]).to.include(res.getStatus());
  });
}
