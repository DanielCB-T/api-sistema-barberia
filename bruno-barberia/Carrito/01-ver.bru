meta {
  name: Ver carrito
  type: http
  seq: 1
}

get {
  url: {{base_url}}/cart
  body: none
  auth: bearer
}

auth:bearer {
  token: {{client_token}}
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
