meta {
  name: Ver carrito
  type: http
  seq: 1
}

get {
  url: http://177.7.32.156/api-sistema-barberia/api/cart
  body: none
  auth: bearer
}

auth:bearer {
  token: {{client_token}}
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
