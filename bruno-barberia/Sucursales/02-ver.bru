meta {
  name: Ver sucursal
  type: http
  seq: 2
}

get {
  url: {{base_url}}/branches/{{branch_id}}
  body: none
  auth: none
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
