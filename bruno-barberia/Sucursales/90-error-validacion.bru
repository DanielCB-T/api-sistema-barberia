meta {
  name: ERROR validación al crear (422)
  type: http
  seq: 90
}

post {
  url: http://177.7.32.156/api-sistema-barberia/api/branches
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Solo nombre"
  }
}

tests {
  test("status 422", function() {
    expect(res.getStatus()).to.equal(422);
  });
}
