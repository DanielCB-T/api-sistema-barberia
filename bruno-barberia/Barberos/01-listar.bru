meta {
  name: Listar barberos (público)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/barbers?branch_id={{branch_id}}
  body: none
  auth: none
}

params:query {
  branch_id: {{branch_id}}
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
