meta {
  name: Actualizar barbero (admin)
  type: http
  seq: 3
}

put {
  url: http://177.7.32.156/api-sistema-barberia/api/barbers/{{barber_id_created}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Barbero Prueba Editado",
    "phone": "9510001122",
    "branch_id": "{{branch_id}}"
  }
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
