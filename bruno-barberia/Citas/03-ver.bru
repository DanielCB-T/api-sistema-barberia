meta {
  name: Ver cita
  type: http
  seq: 3
}

get {
  url: http://177.7.32.156/api-sistema-barberia/api/appointments/{{appointment_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status 200 (o 404 si el id no existe)", function() {
    expect([200,404]).to.include(res.getStatus());
  });
}
