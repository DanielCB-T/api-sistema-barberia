meta {
  name: Listar citas
  type: http
  seq: 1
}

get {
  url: {{base_url}}/appointments?per_page=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  per_page: 10
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("data es arreglo", function() {
    expect(res.getBody().data).to.be.an("array");
  });
}
