meta {
  name: Listar citas
  type: http
  seq: 1
}

get {
  url: http://177.7.32.156/api-sistema-barberia/api/appointments?per_page=10
  body: none
  auth: bearer
}

params:query {
  per_page: 10
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("data es arreglo", function() {
    expect(res.getBody().data).to.be.an("array");
  });
}
