meta {
  name: Cambiar contraseña
  type: http
  seq: 5
}

post {
  url: http://177.7.32.156/api-sistema-barberia/api/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "current_password": "{{admin_password}}",
    "password": "{{admin_password}}",
    "password_confirmation": "{{admin_password}}"
  }
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

docs {
  Usa la misma contraseña como nueva para no dejar bloqueada la cuenta de pruebas.
}
