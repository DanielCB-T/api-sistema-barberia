meta {
  name: Logout
  type: http
  seq: 99
}

post {
  url: http://177.7.32.156/api-sistema-barberia/api/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

docs {
  OJO: revoca el token actual. Ejecutalo al final; luego vuelve a Login Admin.
}
